# Pending Beads reconciliation — 2026-09-22

Beads is unavailable in this session (no bd command in PowerShell or Git Bash;
bounded executable searches did not locate it). Preserve this temporary note
until its contents are reconciled through the live tracker; the issue export was
searched read-only, including closed records.

Matches: ops-cth (UI design guidance), ops-kxf (upstream improvement reference),
and metabrain-mvp-w2w.7 (Themes). Verify live state before updating them.

Owner, Metabrain design discussion, 2026-09-22:
"Design principle and rule: Color is a required information-bearing,
ontology-bearing dimension of meaning in design. You **must** use color
deliberately across the application UI/HUD."

Owner follow-through request:
"Ensure the new design principle is synthesized coherently into the agentic
improvement repo docs, not just locally. Is that instantiated clearly as a
standing instruction? Two-way improvement is supposed to be."

Intent: make deliberate semantic color a standing shared UI requirement and
make the existing upstream contribution/adoption loop explicitly include design
practice documents. Adopt the delivered revision back into Metabrain. Keep
project-specific theme presets and Beads import scope in Metabrain's work record.

Acceptance: shared UI guidance contains the rule, concrete application guidance,
and observable checks; the core routes agents to it and states the two-way
workflow; Metabrain carries the matching adoption and revision; package checks,
adopted checks, and Metabrain document governance succeed; scoped commits are
pushed to both upstreams. Tracker synchronization remains pending until Beads is
available. No runtime UI changes are part of this documentation correction.

Incident: the prior assistant retained the principle only in a local temporary
capture despite the existing upstream-contribution rule. This is an execution
miss, not evidence that the standing rule was absent.
