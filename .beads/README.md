# Work state

Use `bd` to query and update this repository's work. The operating protocol is in
[AGENTS.md](../AGENTS.md); setup and synchronization are in
[docs/beads.md](../docs/beads.md).

`issues.jsonl` is a generated review snapshot. Dolt holds the live database and
history; Git alone does not synchronize it.
